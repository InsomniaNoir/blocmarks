<h1 align="center">Blocmark Topics</h1>
<div class="container">
  <div class="row">
    <% @topics.each do |topic| %>
      <div class="col-md-3">
      <%= link_to "##{topic.title}", topic_path %>
      <%= link_to "X", method: :destroy %>
      </div>
    <% end %>
  </div>
</div>
